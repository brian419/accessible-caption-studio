name: Retired one-time patch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired
        run: echo "The one-time branch patch has already been applied."
