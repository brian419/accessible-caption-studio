name: Apply failed job retry controls

on:
  push:
    branches: [major-iterations]
    paths:
      - scripts/apply_job_retry_ui.py
      - .github/workflows/apply-major-ui.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: major-iterations

      - name: Apply patch
        run: python scripts/apply_job_retry_ui.py

      - name: Commit generated changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/accessible_caption_studio/web/app.js src/accessible_caption_studio/web/styles.css
          if git diff --cached --quiet; then
            echo "No generated changes to commit."
            exit 0
          fi
          git commit -m "Add retry controls for failed processing jobs"
          git push origin HEAD:major-iterations
