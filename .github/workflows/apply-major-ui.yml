name: Apply readability and finding fixes

on:
  push:
    branches: [major-iterations]
    paths:
      - scripts/apply_readability_autofix.py
      - .github/workflows/apply-major-ui.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: major-iterations

      - name: Apply patch
        run: python scripts/apply_readability_autofix.py

      - name: Commit generated changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/accessible_caption_studio/web/app.js src/accessible_caption_studio/web/styles.css
          if git diff --cached --quiet; then
            echo "No generated changes to commit."
            exit 0
          fi
          git commit -m "Add readability warnings and safe finding fixes"
          git push origin HEAD:major-iterations
